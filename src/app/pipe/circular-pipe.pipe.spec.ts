import { CircularPipePipe } from './circular-pipe.pipe';

describe('CircularPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CircularPipePipe();
    expect(pipe).toBeTruthy();
  });
});
